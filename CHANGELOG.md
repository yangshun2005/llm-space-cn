# 更新日志

此处记录了 LLM Space 的所有重要更改。本项目遵循
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) 和
[语义化版本](https://semver.org/spec/v2.0.0.html) 规范。

## [4.18.1] - 2026-09-07

### 新增

- 内置的腾讯 Token Hub 提供商连接到其兼容 OpenAI 的端点，自动发现 `TOKENHUB_API_KEY` 或 `TOKEN_HUB_API_KEY`，并包含当前的 Hunyuan、DeepSeek、GLM、Kimi、MiniMax、Qwen、MiMo 和视觉模型。

### 变更

- 确认 `@earendil-works/pi-ai` 0.85.1 是 2026-09-07 的最新注册表版本，并保留了本地的 Responses 兼容性补丁。

### 修复

- 模型配置文件中的自定义请求头控件和 Anthropic Base URL 指南现在遵循所选界面语言，包括可访问的标签。

## [4.18.0] - 2026-09-07

此版本升级了 Pi 代理运行时，并完成了第一轮广泛的国际化工作，为桌面应用和共享的 Thread Playground 带来了中文界面。

### 新增

- 中文界面语言现已覆盖桌面外壳、设置、对话框和 Thread Playground，支持双语命令面板匹配和本地化的提供商名称。
- 捆绑的跨项目 Memory 插件为代理提供了一个可选的持久化存储，用于保存偏好设置、项目约定和决策。
- 搜索设置现在除了现有的搜索集成外，还包含 Exa、AnySearch 和知乎 MCP 提供商。
- 内置的阿里云 Qwen 提供商使用 DashScope OpenAI 兼容 API，自动发现 `DASHSCOPE_API_KEY`，并包含 Qwen 3.8/3.7 以及选定的 DeepSeek、Kimi、GLM 和 MiniMax 模型。
- 一个短剧写作示例演示了 Thread Playground 中结构化的创意写作工作流程。

### 变更

- 将 `@earendil-works/pi-ai` 和 `@earendil-works/pi-agent-core` 更新至 0.85.1，这是 2026-09-07 检查到的最新注册表版本，同时保留了本地的 Responses 兼容性补丁。

### 修复

- 知乎搜索现在可以处理其 SSE 流上由服务器发起的 JSON-RPC 请求。

## [4.17.0] - 2026-09-05

### 新增

- MCP 添加菜单现在除了自定义服务器设置外，还提供按字母顺序排列的高德地图、Chrome DevTools、Context7、ElevenLabs、GitHub 和 Playwright 预设。
- MCP 凭据指南包括醒目的“获取 API 令牌”链接、必填或可选状态，以及特定于提供商的设置说明。
- macOS 语音内置工具可以列出已安装的声音，使用选定的声音和语速朗读文本，并停止播放而不影响其他应用程序。它们不需要 API 密钥，并出现在 macOS 运行时的“媒体”类别中。

### 变更

- 将 Pi 模型和代理库更新至 0.85.0，同时保留了本地的 Responses 兼容性补丁。

### 修复

- 切换线程标签页会选中并在侧边栏中显示相应的文件，而不会重新打开标签页或移动键盘焦点。
- 向远程服务器 URL 添加密钥后，MCP 凭据指南仍然可见，并且重复的推荐服务器会获得唯一的名称。

## [4.16.0] - 2026-09-03

### 新增

- 一个 `spawn_agent` 内置工具用于请求手动创建子任务。调用会暂停自动执行和 ReAct 循环；创建子任务会打开其标签页并展开其文件夹，而不会启动运行或填充工具响应。
- 子任务继承适合一般工作、研究或代码审查的提示模板、变量、模型设置和工具。可读的任务名称会成为唯一的文件名，并且可以从工具卡片再次创建缺失的子任务文件。
- 一个 `exec_code` 内置工具可以在持久会话中运行 Python、JavaScript 和 TypeScript，在调用之间保留变量和导入。
- `calculator` 和 `date_difference` 内置工具处理数学表达式以及日期或时间间隔，并提供匹配的 Python 导出。

### 变更

- 通用代理模板现在包含 `exec_code`，并将其之前的 `agent()` 函数替换为 `spawn_agent`。
- Python 导出明确说明手动创建子任务需要桌面应用；生成的工具和项目计划解释了此限制。
- 整个应用中的滚动条使用一致的滑块样式。
- 远程运行时协议现在为 v5。请将服务器与桌面应用一起更新以使用此版本。

### 修复

- 性能版中的长对话在页面缩放时保持一致的消息间距。

## [4.15.2] - 2026-08-29

### 修复

- 性能版在恢复页面缩放前等待 Chromium 的 DOM 就绪信号，防止因在 CEF 浏览器存在之前执行 JavaScript 而导致的二次启动崩溃。

## [4.15.1] - 2026-08-29

### 修复

- 引用实时技能变量的提示模板会在启用的技能集更改时刷新，而不是重用过时的整个模板快照。现有的线程快照会在下次运行时自动迁移。
- 当流式传输从底部以外的位置开始，或一次性消息焦点已被消耗后，消息视图不再抢占滚动位置。

## [4.15.0] - 2026-08-27

### 变更

- 性能版现在支持持久的“视图”菜单缩放，并带有视口补偿的 Chromium 渲染。放大在 macOS 上使用更便捷的 `Command+=` 快捷键，同时保留现有的缩小和重置缩放操作。

### 修复

- 退出全屏会恢复最后的普通或最大化窗口状态，而不是保留临时的 macOS 过渡边界。

## [4.14.2] - 2026-08-27

### 变更

- 捆绑的 pi 包升级至 `0.84.3`，同时保留 Responses 原生工具重放、引用和提供商托管的活动元数据。

### 修复

- 消息列表跟随流式输出，不会与用户滚动冲突，并且在标准和虚拟化布局之间切换时不再重用过时的容器测量值。

## [4.14.1] - 2026-08-19

### 修复

- 内置和插件工具导入列表按字母顺序排序，无论大小写，都能更轻松地找到工具。

## [4.14.0] - 2026-08-18

### 新增

- 一个兼容 Codex 的 `apply_patch` 内置工具可以应用经过验证的多文件添加、更新、删除和移动操作，具有上下文匹配功能，并明确指导优先使用它进行文件内容更改。

### 变更

- Bash 工具会引导代理在修改现有文件内容时使用专用的文件编辑工具。

## [4.13.0] - 2026-08-17

此版本使长线程滚动更流畅，将工作目录解析为稳定的绝对路径，并优化了导航和引导反馈。

### 变更

- 工作目录提示变量会展开用户编写的路径，并在桌面运行、提示预览、压缩、代码生成和生成的 LangGraph 项目中，将非空值持久化为绝对路径。
- 网络搜索工具结果显示一个紧凑的四结果预览，可按需展开，而不会引入嵌套的垂直滚动区域。
- 消息导航锚点提供更清晰的焦点和悬停邻近性，而引导设置面板的进入过渡更连贯。

### 修复

- 包含少量大消息的对话避免不必要的虚拟化，而长对话则使用自适应和缓存的高度估计、更宽的预渲染以及合成器支持的定位。这可以防止快速触控板或 Magic Mouse 滚动时出现向上滚动陷阱、滚动条跳动和空白帧。

## [4.12.3] - 2026-08-17

### 修复

- 在短窗口中，对话框操作保持可见，同时编辑器、表单和详细内容在可用视口内滚动或调整大小。
- 引导、共享、功能提醒和 SSH 主机密钥确认会适应降低的窗口高度，而不会裁剪其主要控件。

## [4.12.2] - 2026-08-16

### 修复

- 手动执行一个或多个工具调用不再导致对话视口在成功或失败结果因延迟的编辑器和虚拟列表测量而展开时发生偏移。

## [4.12.1] - 2026-08-14

### 修复

- 性能版再次在启动时加载其打包的渲染器。外观偏好设置会在 React 挂载前从磁盘水合，而不会向 Electrobun 的 `views://` URL 添加不受支持的查询字符串。

## [4.12.0] - 2026-08-14

此版本使生成的媒体持久化，改进更新可见性和模型提供商管理，并稳定长时间运行的流式对话。

### 新增

- 生成的图像和其他媒体会持久化到输出目录，以便在生成工具调用完成后结果仍然可用。
- 更新对话框在获取新的桌面构建版本时报告下载进度。

### 变更

- 模型提供商连接配置文件使用更清晰的分层管理布局和更一致的编辑控件。
- 渲染器偏好设置（包括选定的主题）通过 `settings/local-storage.json` 中的单个磁盘支持的本地存储服务持久化。

### 修复

- 长助手消息在流式传输结束时不再导致对话跳动，并且消息标尺会选择包含视口中心的项目，而不是附近较短的消息。
- 提供商编辑器间距和插件目录操作更加一致。

## [4.11.0] - 2026-08-11

此版本使本地插件可作为 ZIP 包安装，简化插件、MCP 服务器和模型提供商的管理，并改进桌面响应能力和交互细节。

### 新增

- 可以通过将插件 ZIP 包拖放到桌面窗口来安装或更新它们。归档文件会经过验证和提取，并带有大小、条目数和路径安全限制，然后原子地替换已安装的包。
- 已安装的插件可以直接从插件列表中显示、重新加载或卸载，持久化的插件数据保留在可替换的安装目录之外。

### 变更

- 模型提供商连接配置文件显示为嵌套的侧边栏项目，可以更快地切换、添加、配置和删除单个连接。
- 插件和 MCP 服务器行暴露上下文管理菜单，包括无需先打开详细编辑器即可进行连接测试和移除。
- 双语插件指南现在记录了 ZIP 打包、拖放安装、归档安全限制、升级和持久化数据存储。
- 侧边栏调整大小和引导过渡更平滑，而关闭的线程工作区会更积极地释放保留的状态。

### 修复

- 更新操作在“设置”对话框上方保持可见。
- 在可展开的工具调用参数中选择文本时，释放指针不再切换参数的展开或折叠状态。

## [4.10.0] - 2026-08-09

此版本增加了预览优先的渐进式对话压缩，使长线程更易于导航，并将主要线程工作流连接到扩展的双语文档。

### 新增

- 线程可以将较旧的轮次压缩为结构化的、渐进更新的检查点，同时保留可配置数量的最近轮次的原文。压缩在总结前渲染提示变量，支持可重用的每线程指令，在应用前预览结果，并创建一个编号的 `-compact-N.json` 克隆而不是覆盖源线程。
- 用户和助手消息暴露导航锚点，以便在长对话中快速移动。
- 一个完整的 Atlas 插件示例演示了多个技能、MCP 服务器、模型提供商、插件工具、命令、线程存储和设置字段。

### 变更

- 压缩、共享、生成项目和变量对话框为新的或扩展的英文和中文指南提供本地化的帮助链接。
- 共享、项目生成、压缩和变量文档可从用户手册、快速入门、核心概念和仓库指南中链接。
- 捆绑的 pi 包升级至 `0.84.1`。

### 修复

- HTML 预览允许其嵌入的脚本按预期运行。
- Bash 工具从所属的工作区根目录启动，而不是无关的进程目录。
- 消息导航控件保持在菜单和对话框下方，而不是覆盖更高优先级的 UI。

## [4.9.0] - 2026-08-07

此版本增加了 Seedream 图像生成和提供商连接配置文件，使本地插件命令更透明，并将插件和 MCP 设置转变为更清晰、信息更丰富的管理界面。

### 新增

- 火山引擎 Ark 提供商可以配置 Seedream 图像模型，并使用新的 `generate_image` 内置工具使用提供商凭据创建图像。
- 模型提供商支持多个命名的连接配置文件，具有配置文件感知的模型选择、连接测试、流式传输和内置工具执行。
- 插件命令在运行时可以发布进度、成功和失败反馈，其生命周期处理在命令面板和桌面运行时之间共享。
- 插件设置将命令、工具和 MCP 服务器分组到可展开的部分，并带有特定类型的图标和扩展描述。
- MCP 设置暴露拥有每个捆绑服务器的插件，并缓存插件工具发现以避免重复的子进程工作。

### 变更

- 设置空状态在插件、MCP 服务器和远程服务器中使用可重用的动画布局，并优化了选择、对比度、间距和图形处理。
- 引导体验具有更小的图像负载和更精致的布局及交互细节。
- 桌面和 Web 构建现在使用 Vite 8，同时更新了 React、Tailwind CSS、CodeMirror、MCP SDK 和 UI 依赖项。
- 插件开发文档现在涵盖命令反馈 API。

### 修复

- 打开共享的深层链接会可靠地激活桌面窗口，然后再导航到其内容。
- 插件图形保留原生图标形状，长扩展描述保持在设置行内。

## [4.8.5] - 2026-08-06

此补丁版本恢复了打包桌面构建中兼容 OpenAI Responses 的提供商的工具调用延续性。

### 修复

- 工具结果重放提供商的原始调用 ID，不带内部响应项后缀，防止出现 `No tool call found for tool output` 错误。
- 本地的 `pi-ai` 补丁现在在全新依赖安装期间正确应用，与开发和发布构建匹配。

## [4.8.4] - 2026-08-05

此版本将本地插件扩展为一流的代理能力，并刷新了最重要的设置、共享和生成体验。

### 新增

- 本地插件可以贡献可执行的工具，用户可以将其添加到线程中，模型可以像内置工具一样调用它们，并具有稳定的身份、包图标、生命周期隔离、结构化结果和所属线程的变量上下文。
- 插件命令接受带引号的命令面板参数，并可以通过显式上下文 API 检查活动标签页、文件名和线程。
- 插件开发指南现在以英文和中文记录命令和工具，包括发现、上下文、变量、本地运行时限制和错误行为。

### 变更

- 新线程画廊、可运行代理生成器和线程共享流程现在使用更清晰、更专注的布局，并具有精美的浅色和深色外观。
- 账户、插件、技能、模型、MCP 服务器和远程服务器设置现在具有更强的导航、有目的的空状态和一致的列表标题。
- 工作目录控件、插件发现和工具选择提供更直接的反馈和操作，包括在 Finder 中显示现有文件夹。

### 修复

- 打开本地插件文件夹使用原生文件系统显示路径，而不是生成不可用的 shell 命令。
- 插件工具执行保持绑定到拥有该调用的线程，即使标签页更改或多个调用并发运行也是如此。
- 不支持的本地插件工具在远程运行时和 LangGraph 导出期间会被明确拒绝，而不是产生不可用的输出。

## [4.8.3] - 2026-08-05

此补丁版本简化了 macOS 上生成的 LangGraph 项目设置。

### 变更

- 在显式创建生成项目的 `.env` 后，macOS 用户现在会获得一个终端窗口，该窗口使用 `make dev` 启动开发服务器。
- 其他平台、取消的设置和启动失败会继续显示生成的项目，而不会启动进程。

### 修复

- 包含空格或 shell 特殊字符的生成项目路径会安全地传递给终端，并且只有用户授权的生成器目录才能启动。

## [4.8.2] - 2026-08-05

此补丁版本使文件系统工具能够可靠地处理相对于主目录的路径，并改进生成的 LangGraph 开发工作流程。

### 新增

- 生成的 LangGraph 项目包含一个 `Makefile`；运行 `make dev` 以启动 `uv run langgraph dev`。

### 变更

- 文件系统工具模式和生成的 Python 实现一致地记录和展开开头的 `~/` 路径。
- MCP stdio 命令和工作目录在桌面运行时和生成的 LangGraph 项目中都展开相对于主目录的路径。

### 修复

- `write`、`edit`、`read`、`ls`、`tree`、`grep`、`glob` 和 `present_files` 不再将 `~/...` 解释为相对于桌面应用包。
- 可以从工具调用中显示相对于主目录的工件路径，包括 `glob` 目标目录。

## [4.8.1] - 2026-08-05

此补丁版本优化了“设置”布局，并使插件文档更易于访问。

### 变更

- 设置页面使用一致的标题分隔线、内容间距和简洁的描述，同时保持紧凑表单行的预期密度。
- 实验性设置现在与网络设置共享相同的带标签切换布局。
- 插件设置直接链接到插件开发指南。

### 修复

- 远程服务器内容与其标题对齐，而其他分屏设置页面保持正确的顶部间距。

## [4.8.0] - 2026-08-05

此版本引入了受信任的本地插件，可以使用代理能力、命令、模型集成和自定义线程存储来扩展 LLM Space。

### 新增

- 从 `LLM_SPACE_HOME/plugins/` 发现受信任的本地插件，并带有元数据、设置、生命周期控制、兼容性检查和隔离的诊断信息。
- 插件可以贡献技能、MCP 服务器、模型提供商、命令面板命令和线程存储。
- 插件技能可用于本地和远程运行时上的代理，可以单独或按插件启用，而无需修改插件文件。
- 添加了英文和中文插件开发指南，涵盖包布局、扩展、设置、生命周期、诊断和示例。

### 变更

- 设置导航分组为应用、代理和连接，远程服务器、MCP 服务器和网络搜索的名称更清晰