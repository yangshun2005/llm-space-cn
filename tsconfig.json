{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["bun"]
  },
  "include": ["packages/*/src", "packages/*/tests", "scripts"],
  "//": "packages/ui is React/DOM code, typechecked by its own project (see the typecheck script) \u2014 this Bun-flavored config lacks DOM libs, so exclude it here.",
  "exclude": ["packages/ui", "packages/runtime"]
}
